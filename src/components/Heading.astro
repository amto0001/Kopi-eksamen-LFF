---
// Henter parametrene fra Astro.props
const { htmlheading="h3", size="", stylingclasses=""} = Astro.props; 
const Htmlheading = htmlheading;
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
"xtra-large": "text-5xl",
"large": "text-4xl",
"medium": "text-3xl",
"small": "text-2xl",
"xtra-small": "text-xl",
"xtra-xtra-small": "text-lg",
};
---
<Htmlheading class:list={[sizeClasses[size], "font-montserrat font-bold text-primary-50 leading-relaxed", stylingclasses]}><slot /></Htmlheading>

<!-- Eksemepl på 'korrekt' brug af heading = mere bærdygtig kode
const sizeClasses = {
    "xtra-large": "text-5xl md:text-6xl",
    "large": "text-4xl md:text-5xl",
    "medium": "text-3xl md:text-4xl",
    "small": "text-2xl md:text-3xl",
    "xtra-small": "text-xl md:text-2xl",
    "xtra-xtra-small": "text-lg md:text-xl",
  };
  -->